#include <unistd.h>
#include <string.h>
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>



char* decodeString(char* s)
{
    int len = strlen(s);
    char *times = (char *)malloc(len * sizeof(char));
    char *res = (char *)malloc(100000 * sizeof(char));
    int j = -1;
    for (int i = 0; i < len; i++)
    {
        
        

    }
    

}