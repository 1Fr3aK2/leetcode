int max(int a, int b)
{
    return (a > b ? a : b);
}

int longestSubarray(int* nums, int numsSize)
{
    int best = 0;
    int start = 0;
    int end = numsSize - 1;
    int zeros = 0;
    for (int i = 0; i < numsSize; i++)
    {
        if (nums[i] == 0)
            zeros++;
        while (zeros > )
        {
            /* code */
        }
        
        
    }
    



}